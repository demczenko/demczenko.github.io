; copy template

Удалить возможность копирования шаблона руками. при копировании удалять переменные из шаблона.
concatenate templates

; filter projects archive
; check error page after submitting

; fix copy table
; fix name change for table
; fix populate table confirmation

подумать над добавлением переменных через UI.
Думаю лучшим выходом будет парсинг таблиц и переменных с шаблона.

; обработать неизвестный айдишник