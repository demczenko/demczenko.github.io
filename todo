Todo:
  -handle color change for: background and text
  
  Project responsible for saving the changes to colors.
  There should be additional table for saving colors with fields:

  project_id: related to project.
  node: where apply colors.
  style: styles

[
  {
    project_id: 1,
    node: url to object node,
    style: object woth key value pairs related to CSS style
  }
]


host: localhost || 192.168.160.57
port: 5432
username: postgres
password: oturig02

table data update check why duplicated after saving
project archive
remove from table list duplicate and delete project
add components header and footer with variables and tables for that component