1 Project initialized
2 Navigation routes created
3 Handle Eror pages 
4 Create Outlet for nested routes
5 Create Sidebar



When creatin project.
If of this project shared to every column_data.

-- todo
-- create tables when creating template
-- copy table whit all columns


--Handle new item add (sheet modal right side)

Render slugs on project page
Render iframe with rendered HTML and swapped data
Add copy button

Think about JSON


1 проект имеет 16 таблиц.
Каждая таблица создается при создании шаблона.

В Таблицу для первой категории я могу добавить 14 стран.
В Таблицу для второй категории я могу добавить 12 стран.
В Таблицу для третьей категории я могу добавить 10 стран.

Проблема:
Нужно определить нужно ли рендерить данный Slug,
Так как его должна иметь каждая таблица и только после проверки отрисовать его
в опциях рендера проекта.

т.е. пользователь все ещё может добавить разное количество данных, но не сможет их отрендерить.

Нужно проитерироваться по каждой таблице проекта и проверить имеет ли таблица итерируемый slug.
Если имеет то добавляем язык.
Если язык уже есть, пропустить его.
Если не имеет, не добавляем.

И таким образом


duplicate by n times

link, image, plain text add options to columns select
handle the same slug name

"Import conflict"

An item with slug "" already exists

check box (apoply to all repetitive items)

Skip item or Update item